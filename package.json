{
  "name": "associative-dreaming-mcp-server",
  "version": "3.0.0",
  "description": "MCP server for associative dreaming - the Yin to Sequential Thinking's Yang. Non-linear exploration through concept space.",
  "license": "MIT",
  "author": "",
  "type": "module",
  "bin": {
    "associative-dreaming": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "node real-test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.3.3"
  }
}
