{
  "name": "@modelcontextprotocol/server-associative-dreaming",
  "version": "2.0.0",
  "description": "MCP server for associative dreaming - the Yin to Sequential Thinking's Yang. Rhizomatic exploration through concept space.",
  "license": "MIT",
  "author": "",
  "homepage": "https://modelcontextprotocol.io",
  "bugs": "https://github.com/modelcontextprotocol/servers/issues",
  "type": "module",
  "bin": {
    "mcp-server-associative-dreaming": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "node -e \"require('fs').chmodSync('dist/index.js', '755')\"",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "chalk": "^5.3.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^4.0.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.0"
  }
}
